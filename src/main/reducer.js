export const reducer = (state,action) => {
    if (action.type === 'ADD') {

        return {

        }
    }
    if (action.type === 'REMOVE') {

        return {
            
        }
    }

    if (action.type === 'Comment') {
        
    }
}